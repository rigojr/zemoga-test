import React from 'react';
import './styles.scss';

/**
 * Spinner component, css source can be found here: https://projects.lukehaas.me/css-loaders/
 */
const Spinner: React.FC = () => {
  return <div className="loader"></div>;
};

export default Spinner;
